/*
 * HomePolls / Polls / Create Messages
 *
 * This contains all the text for the HomePolls / Polls / Create container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.HomePolls.Polls.Create';

export default defineMessages({
  createPoll: {
    id: `${scope}.createPoll`,
    defaultMessage: 'Create Poll',
  },
});
