import styled from 'styled-components';
import A from 'components/Button';

const CreateMovie = styled(A)`
  border: 2px dashed rgba(0, 0, 0, 0.2);
  background-color: transparent;
  padding: 6px;
  width: 100%;
  display: block;
`;

export default CreateMovie;
