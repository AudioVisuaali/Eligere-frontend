import styled from 'styled-components';

const UrlContaner = styled.div`
  position: relative;
  margin-bottom: 5px;

  & input {
    padding-left: 45px;
    padding-right: 45px;
  }
`;

export default UrlContaner;
