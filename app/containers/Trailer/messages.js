/*
 * Trailer Messages
 *
 * This contains all the text for the Trailer container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.Trailer';

export default defineMessages({
  videoAddress: {
    id: `${scope}.videoAddress`,
    defaultMessage: 'Video address (Youtube)',
  },
});
